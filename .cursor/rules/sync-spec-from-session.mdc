---
description: 会话确认或代码落地后，把约定同步到现行 spec 文档
alwaysApply: true
---

# 会话确认后同步 Spec

当本会话里出现「已确认」的设计决策、行为变更、接口变更，或代码已落地且与现行规格不一致时，必须把规格文档补齐，不能只改代码不改 spec。

## 何时触发

- 用户明确说确认 / OK / 按这个做 / 写进规格
- 功能或修 bug 已实现并通过相关测试，且改变了对外行为、接口、数据模型、存储或产品文案
- 会话约定与 `docs/superpowers/specs/**`（或仓库同等 specs 目录）中的现行文档冲突或缺失

不触发：纯探索、未确认方案、临时调试、未改变约定的重构。

## 怎么做

1. 回顾本会话已确认点与明确「不做」的范围。
2. 对照代码 / git diff，只收录已落地或用户批准写入规格的内容。
3. 更新对应现行 spec；没有则新建 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`。
4. 就地改章节（目标、模型、接口、架构、交互、验收、不做），避免整篇重写。
5. 告知用户改了哪些文件；用户未要求时不要 commit / push。

## 禁止

- 未确认猜想写入 spec
- 用实现细节淹没规格
- 只改历史稿、不改现行稿
